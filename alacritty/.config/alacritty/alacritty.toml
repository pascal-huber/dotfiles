# NOTE: as of version 0.14 import is inside [general]
# [general]
import = [
  "~/.config/alacritty/themes/gruvbox_darker.toml"
]

[font]
size = 12.0

[keyboard]
bindings = [
# fix control-space
{ key = "Space", mods = "Control", chars = "\u0000" }
]
