#!/bin/bash

cd "${HOME}/git/"

for dir in */; do
    cd "${HOME}/git/${dir}"

    if [ ! -d ".git" ]; then
        echo "${dir}: no-git"
    elif  [[ -n $(git status -s) ]]; then
        echo "${dir}: dirty-git"
    else
        echo "${dir}: clean-git"
    fi

done
